{
	"name": "supertest_poc",
	"version": "1.0.0",
	"description": "This is a POC for supertest tool",
	"main": "index.js",
	"scripts": {
		"test": "mocha **/*.spec.js && mocha --reporter json > reports/test-results.json",
		"report": "mocha --reporter mochawesome"
	},
	"keywords": [
		"supertest"
	],
  "devDependencies": {
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "supertest": "^6.3.3"
  },
  "dependencies": {
		"mochawesome": "^7.0.1"
	}
}

