{
  "devDependencies": {
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "supertest": "^6.3.3",
    "scripts": {
      "test": "mocha **/*.spec.js && mocha --reporter json > reports/test-results.json",
      "report": "mocha --reporter mochawesome"
    }
  }
}
